{
  "name": "postgres",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "drop": "sequelize db:drop",
    "create": "sequelize db:create",
    "migrate": "sequelize db:migrate",
    "seed": "sequelize db:seed",
    "init": "npm run create && npm run migrate && npm run seed",
    "reset": "npm run drop && npm run init"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.9.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.29.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.6.0"
  }
}
