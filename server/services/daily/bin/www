const app = require('../app')
const { connect } = require('../mongo/config/db')
const port = 3000

connect().then(() => {
  app.listen(port, () => {
    console.log('app listen in port', port)
  })
})