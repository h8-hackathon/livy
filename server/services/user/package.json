{
  "name": "user",
  "version": "1.0.0",
  "description": "- `GET /users`\r - `GET /users/:id`\r - `POST /users`\r - `PUT /users/:id`\r - `DELETE /users/:id`\r - `PATCH /users/:id`",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon ./bin/www",
    "start": "node bin/www",
    "test": "jest --forceExit --detectOpenHandles --coverage",
    "reset-test": "sequelize db:drop --env test && sequelize db:create --env test && sequelize db:migrate --env test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.9.0",
    "sequelize": "^6.29.0"
  },
  "devDependencies": {
    "jest": "^29.4.3",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  }
}
