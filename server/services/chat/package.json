{
  "name": "chat",
  "version": "1.0.0",
  "description": "Disini chat service menghandle chat dari user ke counselor dan user ke livy.",
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "dev": "nodemon bin/www",
    "test": "jest --forceExit --coverage --testTimeout 5000",
    "reset-test": "sequelize db:drop --env test && sequelize db:create --env test && sequelize db:migrate --env test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jest": "^29.5.0",
    "mongodb": "^5.1.0",
    "openai": "^3.2.1",
  
    "supertest": "^6.3.3"
  }
}
