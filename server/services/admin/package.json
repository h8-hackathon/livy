{
  "name": "admin",
  "version": "1.0.0",
  "description": "### Enpoints posts - `GET /posts` - `GET /posts/:id` - `POST /posts` - `PUT /posts/:id` - `DELETE /posts/:id` counselors - `GET /counselors` - `PATCH /counselors/:id` - `DELETE /counselors/:id` reports - `GET /reports` - `POST /reports` - `DELETE /reports`",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon bin/www"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.9.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.29.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
